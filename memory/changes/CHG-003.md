# CHG-003: 開発タスクをjustfileへ移行してマルチプラットフォーム化した

Status: SHIPPED
Reason: ADR-002
Change: PowerShell依存の `Makefile` を廃止し、`justfile` へ移行した。`build` `test` `init` `new-*` `exec-*` `run` `clean` をクロスプラットフォームなレシピとして再定義し、`mise.toml` に `just` を追加した
FilesChanged: justfile, mise.toml, memory/decisions/ADR-002.md
Verification: `go test ./...` `just --list --unsorted` `just test` `just build`
