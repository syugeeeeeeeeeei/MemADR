# CHG-007: GitHub Actions経由の配布リリース導線を追加した

Status: SHIPPED
Reason: ADR-006
Change: `buildctl release` でタグ作成後に origin へ push するようにし、`buildctl dist` と GitHub Actions の release workflow を追加して Windows、macOS、Linux 向けバイナリと checksums を GitHub Release へ載せる導線を作った。あわせて `version` 表示を `vX.Y.Z` 基準へ整理し、詳細状態は verbose 表示へ分離した
FilesChanged: .github/workflows/release.yml, cmd/buildctl/**, internal/buildinfo/**, internal/release/**, internal/app/**, justfile, memory/decisions/ADR-006.md
Verification: `go test ./...`、`go run ./cmd/memadr version`、`go run ./cmd/memadr version --verbose`、`go run ./cmd/buildctl dist -version v0.0.1 -out-dir <temp>`
