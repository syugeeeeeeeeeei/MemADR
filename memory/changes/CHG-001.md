# CHG-001: `init` と `new` を開始実装した

Status: SHIPPED
Reason: ADR-001
Change: Go CLIの初期構成を追加し、`memadr init` と `memadr new` の最小実装、およびビルドと実行をまとめる `Makefile` を入れた
FilesChanged: cmd/memadr/**, internal/**, Makefile, docs/20260617_start_tool_creation/**
Verification: `go test ./...` と `make build` 相当のビルド、および `make init` の実行経路を確認した
