# ADR-001: 初期実装は標準ライブラリ中心の薄いCLIで開始する

Status: ACCEPTED
Context: 企画書と仕様書はMarkdown正本と薄いCLIを前提にしており、初期版では過剰な機能追加を避ける必要がある
Decision: 初回実装はGo標準ライブラリ中心で `init` と `new` から着手し、CLI層と内部ロジックを分離する
Rejected: 初手から全コマンドやSQLite検索まで同時実装する案
Consequence: 早期に動く最小コマンドを作れる一方で、`check` `list` `index` は次段階で追加する
