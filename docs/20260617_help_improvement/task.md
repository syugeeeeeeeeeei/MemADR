# タスク

## 目的

`memadr` を引数なしで実行したときに、実用的なヘルプを表示する。

## 今回の要求

- `memadr` 単体実行で help を表示する
- `help` をかなり作り込む
- 将来コマンド追加や仕様変更があっても、ヘルプを編集しやすい構成にする

## 完了条件

- `memadr` 実行時に総合ヘルプが表示される
- `memadr help new` のように個別ヘルプを表示できる
- コマンド定義とヘルプ定義が分散しすぎていない
- テストでヘルプ表示を確認できる
