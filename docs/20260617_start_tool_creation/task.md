# タスク

## 目的

`MemADR_企画書.md` と `MemADR_仕様書.md` を踏まえて、`MemADR` の実装を開始する。

## 今回の着手範囲

- Go製CLIプロジェクトの初期構成を作る
- 開発自体のADRを開始する
- `memadr init` と `memadr new` を最初の実装対象にする
- 先にテストを書いてから実装する

## 完了条件

- ローカルでGoテストを実行できる
- `memadr init` のテストが追加されて通る
- `memadr new bug` と `memadr new adr` のテストが追加されて通る
- 今回の作業内容を `walkthrough.md` に記録する
